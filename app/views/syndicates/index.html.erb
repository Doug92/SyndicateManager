<h1>Syndicate List</h1>

<table>
  <tr>
    <th>Syndicate id</th>
    <th>Syndicate name</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @syndicates.each do |syndicate| %>

  <tr>
    <td><%= syndicate.id %></td>
    <td><%= syndicate.syndicate_name %></td>
    <td><%= syndicate.status %></td>
    <td><%= link_to 'Edit', edit_syndicate_path(syndicate) %></td>
    <td><%= link_to 'Delete', syndicate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <td><%= link_to 'Approve', approve_url(:id => syndicate.id), data: { confirm: 'Are you sure you want to approve this new Syndicate' } %></td>
    <td><%= link_to 'Reject', reject_url(:id => syndicate.id), data: { confirm: 'Are you sure you want to reject this new Syndicate?' } %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Syndicate', new_syndicate_path %>
